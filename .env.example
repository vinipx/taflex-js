# ==========================================
# 🚀 TAFLEX-JS CONFIGURATION
# ==========================================
# This file serves as a template for your .env file.
# Copy this file to .env and update the values as needed.
#
# Sections:
# 1. CORE CONFIGURATION
# 2. WEB AUTOMATION
# 3. API AUTOMATION
# 4. MOBILE AUTOMATION
# 5. CLOUD EXECUTION (BrowserStack / SauceLabs)
# 6. DATABASE CONFIGURATION
# 7. REPORTING (Allure, ReportPortal, Xray)
# 8. CONTRACT TESTING (Pact)
# ==========================================

# ==========================================
# 1. CORE CONFIGURATION
# ==========================================
# Execution Mode:
# - web    : Runs UI tests using Playwright
# - api    : Runs API tests using Playwright or Axios
# - mobile : Runs Mobile tests using WebdriverIO (Appium)
EXECUTION_MODE=web

# Timeout (ms) for tests and assertions
TIMEOUT=30000

# ==========================================
# 2. WEB AUTOMATION
# ==========================================
# Browser to use for web tests.
# Options: chromium, firefox, webkit
BROWSER=chromium

# Run in headless mode (true) or visible mode (false)
HEADLESS=true

# Base URL of the web application under test
BASE_URL=https://www.google.com

# ==========================================
# 3. API AUTOMATION
# ==========================================
# Base URL for API testing endpoints
API_BASE_URL=https://jsonplaceholder.typicode.com

# Underlying library for API requests
# Options: playwright, axios
API_PROVIDER=playwright

# ==========================================
# 4. MOBILE AUTOMATION
# ==========================================
# Note: For Mobile, we reuse OS and OS_VERSION variables.
# OS          -> platformName (e.g., Android, iOS)
# OS_VERSION  -> deviceName   (e.g., Google Pixel 7, iPhone 14)

# ==========================================
# 5. CLOUD EXECUTION
# ==========================================
# Platform provider for running tests in the cloud.
# Options: local, browserstack, saucelabs
CLOUD_PLATFORM=local

# Cloud Credentials (Required if CLOUD_PLATFORM is not 'local')
CLOUD_USER=your_username
CLOUD_KEY=your_access_key

# Environment Configuration
# For Web: Operating System (e.g., Windows, macOS)
# For Mobile: Platform Name (e.g., Android, iOS)
OS=Windows

# For Web: OS Version (e.g., 10, 11, Monterey)
# For Mobile: Device Name (e.g., Google Pixel 7)
OS_VERSION=11

# Browser Version (for Web Cloud Execution)
BROWSER_VERSION=latest

# Optional: Remote URL for custom grid
# REMOTE_URL=http://localhost:4444/wd/hub

# ==========================================
# 6. DATABASE CONFIGURATION (Optional)
# ==========================================
# DB_TYPE=postgres
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=user
# DB_PASS=pass
# DB_NAME=mydb

# ==========================================
# 7. REPORTING
# ==========================================
# Comma-separated list of reporters.
# Options include: html, allure, reportportal, junit, json
REPORTERS=html,allure

# Directory for Allure results
ALLURE_RESULTS_DIR=allure-results

# --- ReportPortal Configuration ---
# Required if 'reportportal' is included in REPORTERS
RP_ENDPOINT=https://your-reportportal-instance/api/v1
RP_API_KEY=your-api-key
RP_PROJECT=your-project
RP_LAUNCH=taflex-js-launch
RP_ATTRIBUTES=browser:chromium;env:dev
RP_DESCRIPTION=Test execution for taflex-js

# --- Xray Configuration ---
# Set to 'true' to enable Xray integration (Jira)
XRAY_ENABLED=false
XRAY_CLIENT_ID=your-client-id
XRAY_CLIENT_SECRET=your-client-secret
XRAY_PROJECT_KEY=PROJ
XRAY_TEST_PLAN_KEY=PROJ-123
XRAY_TEST_EXEC_KEY=PROJ-456
XRAY_ENVIRONMENT=Production

# ==========================================
# 8. CONTRACT TESTING (Pact)
# ==========================================
# Set to 'true' to enable Pact contract testing features
PACT_ENABLED=false

# Pact Broker Configuration
PACT_BROKER_URL=https://your-pact-broker.com
PACT_BROKER_TOKEN=your-token

# Consumer and Provider Names for Contracts
PACT_CONSUMER=my-web-app
PACT_PROVIDER=my-api-service

# Logging Level for Pact
# Options: debug, info, warn, error
PACT_LOG_LEVEL=info
